Password if you locked activeOS: 4657 | This is not a real OS. The pretend OS will be in full screen.    | To exit activeOS: Go to terminal and type in (exit activeOS) This command will exit activeOS and go to the OS that you are really using   |
                                                         commands in activeOS terminal: (lock activeOS) (exit activeOS)    (echo {text})

